[pytest]
# Repo root on sys.path so tests can `import config`, `import options_fetcher`,
# `import providers`, etc. without packaging the app.
pythonpath = .
testpaths = tests
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
